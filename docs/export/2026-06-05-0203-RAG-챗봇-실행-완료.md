시스템이 정상 기동했습니다. 전체 RAG 챗봇 스택이 라이브 상태입니다.

## ✅ 실행 완료

| 구성요소 | 상태 | 주소 |
|---------|------|------|
| **백엔드** (FastAPI + Qwen2.5-7B MLX + Chroma) | 🟢 가동 | http://127.0.0.1:8000 (`/health` → ok) |
| **프론트엔드** (Next.js 16) | 🟢 가동 | **http://localhost:3000** |

### 검증된 동작
- **RAG 파이프라인 정상**: `/chat` 스모크 테스트에서 ①상황 요약 ②적용 법리 형식으로 구조화 답변 + 법령 인용(「민사집행법」제41조 등)을 SSE 스트리밍 (모델 로딩 포함 첫 응답 ~23초)
- Chroma 인덱스(12M, `jeonse_deposit` 컬렉션), MLX 모델 캐시, 웹 `.env.local`(`NEXT_PUBLIC_API_BASE=http://localhost:8000`) 모두 정상

### 사용 방법
브라우저에서 **http://localhost:3000** 을 열고 보증금 관련 질문을 입력하면 됩니다. (첫 질문은 모델 로딩으로 느릴 수 있으나, 방금 스모크 테스트로 이미 워밍업되어 다음 호출부터는 빠릅니다.)

### 참고
- 두 서버 모두 백그라운드로 실행 중입니다 (백엔드 `br7oiroe6`, 프론트 `b50dep70u`). 종료하려면 말씀해 주세요.
- 현재는 **QLoRA 적용 전 RAG 베이스라인**입니다(A/B 결과상 소규모 self-distillation 어댑터는 베이스라인 대비 소폭 하락이라 미적용 상태). 어댑터를 붙여 실행해 보고 싶으시면 안내해 드리겠습니다.

브라우저 데모 화면을 직접 확인하거나(`/verify`), 특정 질문으로 테스트해 보길 원하시면 말씀해 주세요.
